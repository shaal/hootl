# Test Conventions

Tests use Node.js built-in test runner (`node:test`). Test files live in `src/test/` and compile to `dist/test/`.

```bash
npm run test:build   # build + run all tests
npm run test         # run tests (requires prior build)
```

## Test Coverage

- **budget.test.ts** -- Global budget: CSV parsing, date filtering, threshold checks
- **config.test.ts** -- Config loading, 3-layer merging, env variable overrides, coercion
- **context.test.ts** -- Context formatting, section inclusion/omission based on null/empty fields, ordering
- **invoke.test.ts** -- Arg building, cost parsing (`total_cost_usd` / `cost_usd`, new-style `usage.costUSD` fallback, top-level preference over nested), `isClaudeEnvelope` detection (old-style cost fields, new-style usage.costUSD, structural markers with 2+ threshold, single marker rejection, normal output rejection, real failure case reproduction), text extraction from JSON (envelope leak prevention: non-string result with cost fields returns empty string, without cost fields returns raw, new-format envelope with null/non-string result returns empty string, new-format envelope with string result extracts it)
- **invoke-robustness.test.ts** -- Timeout handling, `is_error` detection, edge cases, `isTransientError` detection (timeout exit code, rate limit, 429, ECONNREFUSED, ENOTFOUND, ETIMEDOUT, ECONNRESET, success bypass, non-transient bypass, case insensitivity), retry constants and backoff pattern verification
- **local-backend.test.ts** -- Task CRUD, filtering, atomic writes
- **loop.test.ts** -- Review JSON parsing (inline, code-block, nested, remediationPlan, breakdown extraction with defaults and type coercion, remediationItems parsing with category/title validation, non-string diffMarkers filtering, weight defaults, combined prose-curly-braces-and-nested-code-fences extraction via reverse brace-matching), buildScoringTable (sorted by potential gain descending, FIX THIS marker at >= 2 point gain, correct weighted point math, header with confidence/target, Work on these first summary), verifyRemediationMarkers (empty items pass, below-threshold weight skipped, empty diffMarkers skipped, all markers found passes, missing markers fails, any-of-multiple markers passes, getDiff failure passes gracefully, multi-item missing list, exact threshold boundary, below-threshold boundary, cwd threading to getDiff), prompt building, preflight integration (understanding.md in execute prompt), confidence regression detection, global budget integration, preflight subtask priority parsing, too_broad subtask auto-creation (priority inheritance, ready state, parent ready with dependencies, understanding.md cleanup, dependency accumulation), remediation decomposition subtask auto-creation (subtask creation with correct title/description/priority/type, weight-descending sorting, 5-subtask cap with overflow merging, branch/worktree inheritance, goal inheritance including null, fractional userPriority assignment, no userPriority when parent has none, chained sequential dependencies, parent updated with subtask deps and blocker note, existing parent deps preserved, understanding.md preservation, diffMarkers in description when present and omitted when empty, return value verification, exact-5-items without merge, single-item no-chaining, existing parent blockers preserved), config gate (remediation.decompose default true, disable via config, one-time guard blocker note check), handleConfidenceMet hook integration (blocking failure with fixes returns in_progress, blocking failure without fixes moves to blocked, hook execution error moves to blocked, context forwarding, cost logging with trigger label, re-verification on fixes_applied with above-target confidence, re-verify confidence drop returns in_progress with remediation plan, re-verification capped at MAX_REVERIFICATIONS, no re-verification when no fixes_applied, re-verify auto-commits hook changes via hookDeps.commit, re-verify cost logged with re-verify phase label), fireHooks (context propagation, no-op on empty hooks, error swallowing), moveToBlocked (on_blocked hook firing, error resilience, blocker forwarding), buildReviewPrompt (branch-aware three-dot diff with opts, plain git diff fallback without opts, fallback on partial opts, previous test results inclusion with branch context), checkAllDependenciesDone (all done returns true, any not-done returns false, getTask error returns false, empty deps returns true), logEvent integration in handleConfidenceMet (blocking hook failure emits state_change and hook_run, re-verification emits phase_start/phase_end, pr_created decision and state_change to review for pr mode), logEvent integration in moveToBlocked (state_change emitted with blocker reason)
- **git.test.ts** -- Slugify edge cases, branch name construction, getHeadSha, resetToSha rollback, getMergedOrGoneBranches (gone, merged, unmerged, mixed batch, empty input), generateCommitMessage (Claude-generated with prefix, whitespace stripping, fallback on throw/empty, diff truncation, multi-line enforcement, 120-char cap, stat summary in prompt, phase in fallback), commitTaskChanges (DI-based diff capture, fallback on failure, no-op on clean, explicit message bypass, excludeFiles targeted staging, excludeFiles all-pre-existing returns false), getDirtyFiles (modified+untracked files, clean working tree), createWorktree (new branch, existing branch, reuse existing path), removeWorktree (removes existing, no-op on missing), worktreeExists (valid worktree, non-existent path, non-worktree directory), git operations with cwd (commitTaskChanges via worktree, getHeadSha/resetToSha via worktree), createTaskBranch stale detection (reset when behind by more than threshold, keep at exact threshold, keep below threshold, selective artifact cleanup preserving non-artifact files, backward compat without opts), hasBranchDiff (no diff returns false, has diff returns true, git error returns true conservatively)
- **sync.test.ts** -- Review task sync: branch merged+deleted promotes to done, branch merged but exists promotes to done, unmerged branch stays review, null branch skipped, no review tasks returns 0, worktree field cleared on promotion (best-effort removeWorktree + null update), worktree cleared on gone (force-deleted) branch promotion, no-worktree task promotes normally
- **reconcile.test.ts** -- Reconcile command (DI via ReconcileDeps for removeWorktree): task with commits on main marked done, task without commits unchanged, stale branch cleanup on reconcile, worktree cleanup on reconcile (tracking mock verifies correct path + worktree field nulled), worktree error resilience (removeWorktree throw → task still done, worktree field not nulled), orphaned worktree directory detection and cleanup (tracking mock verifies call), orphaned worktree for non-existent task detected and cleaned, dry-run produces no side effects (state unchanged, removeWorktree not called), dry-run does not delete branches (branch still exists after), already-done tasks skipped, multi-state reconciliation (proposed, ready, blocked all reconciled). Report output: empty result prints "Nothing to reconcile", non-empty result prints table with correct entries/header/counts, dry-run prefix "[DRY RUN]" included in header and success/warn messages, empty dry-run includes prefix, orphan-only result (no reconciled tasks) prints only orphan warning, long title truncation with ellipsis
- **discuss.test.ts** -- buildDiscussArgs, system prompt construction, section ordering
- **dependencies.test.ts** -- Dependency inference (explicit indices, heuristic fallback, cycle detection, out-of-range filtering), keyword extraction, index-to-ID resolution
- **guided.test.ts** -- Clarification prompt building, question JSON parsing (valid, malformed, capped), constraints formatting, edge cases
- **plan-memory.test.ts** -- Memory entry generation (success/blocked variants, blocker categorization, truncation), append/rotation (50-entry cap, FIFO), pattern loading (recent count, empty file), metrics computation (averages, completion rate, blocker reasons), prompt formatting
- **preflight.test.ts** -- Template existence, role declaration, verdict values, JSON output fields, no-implementation constraints, bug reproduction instructions, scope assessment
- **extract-tasks.test.ts** -- JSON array extraction from Claude plan responses: clean JSON, markdown code blocks (with/without json tag), preamble text, [bracketed] prose after JSON (the original bug), [bracketed] prose before JSON, escaped quotes in descriptions, nested dependsOn arrays, null cases (empty string, no JSON, empty array, non-array), group field preservation (present, absent/undefined, mixed with/without)
- **plan-review.test.ts** -- Critique prompt building (goal inclusion, task JSON, indices, dependsOn), task parsing (valid, markdown-wrapped, missing fields, non-integer deps), fallback on invalid input
- **plan-summary.test.ts** -- Summary generation (single/multiple/many tasks, truncation), priority counting (mixed, default-to-medium), empty array, priority ordering
- **hooks.test.ts** -- Trigger filtering (condition evaluation, minConfidence), prompt resolution (inline vs file path, fallback), result parsing (JSON extraction, multi-candidate brace-matching with reverse priority, code-block extraction, prose with curly braces before JSON, graceful degradation, new field aliases: passed/fixes_applied/confidence, Claude JSON envelope detection: total_cost_usd/cost_usd variants, plain-text inner result, non-object inner result, no false positive on normal output with result field, empty string result returns pass true, whitespace-only result returns pass true, null result with envelope markers returns pass true, non-string result with envelope markers returns pass true, no false positive without envelope markers, new-format envelope with usage.costUSD: string result extraction, null result pass-through, non-string result pass-through, structural marker-only detection), system prompt construction, runHook integration (pass/fail, cost, context forwarding, verbose always false for prompt-based hooks), runSkillHook (verbose always false regardless of skill options), runHooks orchestration (blocking short-circuit, advisory continues, cost logging, trigger filtering), validate-simplify template (existence, content markers, variable substitution), buildTestHookContext (synthetic task defaults, parameter passthrough, config forwarding, ISO timestamps, edge confidence values), formatHookLabel (skill/prompt display, truncation, 1-based numbering, skill precedence), validateRemoveIndex (valid/invalid/boundary indices, NaN, empty list, single-hook), saveProjectConfig (hooks splice, last-hook removal, empty config, JSON formatting), hooks add config mutation (append to existing array, create array when absent, conditions with minConfidence, preserve existing config keys), HOOK_TRIGGERS export (values, HookSchema acceptance)
- **prioritize.test.ts** -- userPriority schema backward compat, sort order (userPriority before auto), dependency enforcement (findRunnableTask)
- **checkpoint.test.ts** -- Checkpoint write (valid JSON, atomic overwrite, error resilience), read (valid file, missing file, invalid JSON, missing fields, wrong types), clear (removes file, no-op if missing), round-trip (write+read, clear+read)
- **auto-init.test.ts** -- Init directory creation, no-op on existing, config defaults, interactive hook prompt (accept/decline), hooks-example.json content, template presets (web-app lower confidence + agent-browser hook, cli-tool standard defaults, library higher confidence + more attempts), unknown template rejection, template hook prompt skipping, TEMPLATE_NAMES export
- **auto.test.ts** -- Auto command task selection loop (empty queue, sequential picks, in_progress preference, dependency skipping), budget gate (exceeded stops, headroom continues, missing CSV), level validation (all four levels accepted by config schema), idle wait retry (retry when other instances active, timeout after max retries, immediate exit when no instances, boundary retry at max-1, constants export), parallel worktree gate (blocks on multi-instance without worktrees, allows single instance, allows multi-instance with worktrees, activeCount passthrough, error propagation from getActiveInstances)
- **branch-block.test.ts** -- Integration test: dirty worktree blocks task on branch switch (real git repo), dirty worktree does NOT block in worktree mode (isolation verification), clean worktree proceeds past branch creation
- **loop-context-window.test.ts** -- Integration test: review phase runs even when execute reports high contextWindowPercent above the configured limit (fake claude executable, real git repo, full runCompletionLoop flow)
- **loop-logging.test.ts** -- Integration test: structured event logging from runCompletionLoop (fake claude executable, real git repo, full runCompletionLoop flow). Verifies run_start is first event with config snapshot (budgetGlobal, confidenceTarget, onConfidenceMode, maxAttempts, useWorktrees), state_change to in_progress, phase_start/phase_end pairs for plan/execute/review with proper ordering and timing data (costUsd, durationMs, exitCode, outputLength), decision: confidence_met, state_change to review (onConfidence: "none"), all events carry correct taskId and timestamps
- **auto-promote.test.ts** -- Integration test: auto-promotion of parent tasks whose subtasks are all done (real git repo, fake claude, full runCompletionLoop flow). Auto-promotes with zero diff and all deps done and confidence > 0 (only preflight called, confidence 100, state review). Does NOT auto-promote fresh task (attempts=0). Does NOT auto-promote when previous attempt failed (confidence=0, attempts > 0 — prevents false promotion of tasks whose execute phase failed before review). Does NOT auto-promote when dependency is not done (loop runs normally). Does NOT auto-promote when branch has real diff despite deps done (loop runs normally). Hook skip on empty diff: skips on_confidence_met hooks when branch has no diff (3 claude calls — plan+execute+review, no hook call)
- **skip-to-hook.test.ts** -- Integration test: skip plan+execute when confidence already met on a clean, unchanged branch (real git repo, fake claude, full runCompletionLoop flow). Fast path triggered: confidence >= target + clean working tree + HEAD matches last_review_sha.txt → skips to handleConfidenceMet (at most 1 claude call for advisory hook, skip_to_hook decision logged). Fast path NOT triggered when confidence below target (plan+execute+review run normally, no skip_to_hook event). Fast path NOT triggered when working tree is dirty (plan+execute+review run normally despite confidence >= target, no skip_to_hook event). Fast path NOT triggered when there are new commits since last review (HEAD differs from stored last_review_sha.txt despite confidence >= target and clean tree, plan+execute+review run normally, no skip_to_hook event)
- **remediation-decomposition.test.ts** -- Integration test: remediation decomposition into subtasks when execute phase fails (real git repo, fake claude binary, full runCompletionLoop flow). Decomposes remediation items into subtasks when execute fails (4-phase progression: plan → execute ok → review below target with 3 remediation items → execute fails; verifies 3 subtasks created in ready state with chained sequential dependencies, parent gets decomposition blocker note, remediation_decomposed decision event logged, loop breaks after exactly 4 claude calls). Does NOT decompose when blocker already contains decomposition note (one-time guard prevents infinite decomposition loops). Does NOT decompose when config.remediation.decompose is false (config gate respected)
- **notify.test.ts** -- OS notification: config gating (osNotify false → no-op), platform detection (darwin → osascript, linux → notify-send, win32 → no-op), error resilience (execa failure swallowed), osascript quote/backslash escaping, linux raw passthrough. Webhook: no-op on null/empty webhook URL, correct POST payload and headers, error resilience (fetch throw, non-2xx), null confidence handling
- **logs.test.ts** -- readLogEntries (valid JSONL parsing, malformed line skipping, ENOENT returns empty, empty file, whitespace-only file, trailing newline, non-ENOENT re-throw), filterEntries (no filters returns all, taskId filter, eventType filter, AND logic stacking, no matches returns empty), formatTimeline (all 9 event types: run_start budget/target, phase_start phase/attempt, phase_end with/without duration, state_change with/without reason, decision with/without details, error phase/message, hook_run pass/fail, budget_check ok/exceeded, rollback truncated SHAs, HH:MM:SS timestamp format, taskId in output, empty input), logsCommand (default limit of 50, custom limit, missing file shows "No matching events", filter miss shows "No matching events", task ID filtering, event type filtering)
- **logger.test.ts** -- LogEvent discriminated union (all 9 event types: phase_start, phase_end, state_change, decision, error, hook_run, budget_check, run_start, rollback), JSONL append with newline terminator, directory creation (recursive: true), injected timestamp via nowFn, sessionId in entries, error resilience (appendFn and mkdirFn failures never throw), multi-event JSONL format, correct file path (events.jsonl), getSessionId consistency, _setSessionId override, run_start config snapshot fields, rollback SHA before/after with reason, phase_end extended fields (durationMs, exitCode, outputLength)
- **claim.test.ts** -- Claim success (file created, PID correct, state transition to in_progress), conflict (second claim with live PID returns false, state unchanged), stale claim cleanup (dead PID removed, re-claim succeeds), release (removes .claim file), release no-op (unclaimed task), findAndClaimTask (claims first runnable, skips already-claimed, retries on conflict, respects dependencies, returns undefined when all claimed)
- **instances.test.ts** -- registerInstance (file creation with correct JSON content, directory auto-creation, trailing newline), deregisterInstance async (removes file, explicit deps override, no-throw on missing file, no-op without stored dir), deregisterInstanceSync (removes file synchronously, no-throw on missing), getActiveInstances (live PID detection, stale PID cleanup removes files, mixed live/dead handling, non-existent directory, empty directory, corrupt JSON removal, missing field removal, non-JSON file ignored, type validation for pid/startedAt/level)
- **goals.test.ts** -- GoalSchema (validation, description default, missing id/title rejection), slugifyGoalId (title case, multi-word, collapsed spaces/dashes, empty string, whitespace-only, special characters, single word, leading/trailing hyphens), TaskSchema goal backward compat (null default when absent, explicit string preserved, explicit null preserved), loadGoals (missing file, invalid JSON, non-array JSON, valid goals, empty array), saveGoals (round-trip, directory creation, overwrite, pretty-print with trailing newline, empty array)
- **plan-clustering.test.ts** -- createGoalsFromGroups: unique groups create goals and assign tasks, no-op when no group fields, mixed grouped/ungrouped tasks, duplicate group deduplication, pre-existing goal skip, slugification applied, empty/whitespace group ignored, missing indexToId mapping handled, first-occurrence title preserved for same slug. JSON_SCHEMA_INSTRUCTION prompt construction: group field included, functional-area label description, goal clustering mention, all required fields present, dependsOn optionality
- **raw-output-log.test.ts** -- saveRawOutput unit tests (directory creation, filename patterns for all phases, verbatim content preservation, graceful degradation on write failure, overwrite on same phase+attempt, concurrent mkdir safety), integration tests (full runCompletionLoop with fake claude: verifies plan/execute/review/hook log files exist with correct content, error-path saves output on failed phases with non-zero exit code, multi-attempt hook uniqueness across attempts with distinct content)
- **status.test.ts** -- readClaimFile (valid file, missing file, corrupt non-JSON, missing pid field, wrong pid type, non-existent directory), isProcessAlive (current process alive, dead PID), getActiveInstances (no claims, live PIDs, dead PIDs, mixed live/dead, pids map keyed by task ID, non-existent directory, corrupt claim files), writeStatusSummary with claim info (Active instances header line, PID annotation for in_progress tasks, no annotation without claim, backward compat without claimInfo, no PID on review-state tasks), writeStatusSummary with goals (goal headers with done counts, multiple goals in registry order, ungrouped tasks, unknown goal ID treated as ungrouped, empty goals omitted, flat fallback when goals undefined, flat fallback when goals empty, all tasks ungrouped when none match)
